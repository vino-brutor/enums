enum Planets {
    MERCURIO = 'mercurio',
    VENUS = 'venus',
    TERRA = 'terra',
    MARTE = 'marte'
}
//vai retoner a string
//por padrão ele vai começar por zero e ir aidicionando


Planets.TERRA

enum Roles{
    ADMIN,
    USER
}

Roles.ADMIN //vai retornar zero